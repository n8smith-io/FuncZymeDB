# Copy this file to .env only when host-specific overrides are needed.
# Keep credentials and machine-specific paths out of version control.

#FZ_DATA_ROOT=/path/to/local/data
#FZ_RESULTS_ROOT=/path/to/local/results
#HF_HOME=/path/to/model/cache
#TORCH_HOME=/path/to/model/cache
